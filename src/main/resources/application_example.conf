cassandra {
  host = "localhost"
  keyspace = "prod"
}

webserver = {
  endpoint = "localhost"
  port = 8080
}

nexmo {
  api_key = <YOUR API KEY>
  api_secret = <YOUR API SECRET>
}

akka.http.session {
  server-secret = <YOUR SERVER SECRET (any long enough random sequence)>
  encrypt-data = false
  max-age = 1 d
    refresh-token {
      max-age = 1 d
    }
}

